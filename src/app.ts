import express, { Express } from 'express';import {Server} from 'http';export class App {    app: Express;    PORT: number;    server: Server;    HOST: string = 'localhost';    constructor(){        this.app  = express();        this.PORT = 8000;    }    useRoutes() {        this.app.get('/', (req,res)=>{            res.status(200).json({message:'Hello'})        });    }    public async init() {        this.useRoutes();        this.server = this.app.listen(this.PORT)        console.log(`server run on https://${this.HOST}:${this.PORT}`)    }}